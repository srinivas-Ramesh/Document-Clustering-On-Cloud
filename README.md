# Document-Clustering-On-Cloud


This is a project which hosts the document analyzer project on cloud and exposes its apis as RESTful services. Document analyzer is built using Apache Lucene Text analyzer. 
